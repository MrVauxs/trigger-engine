<div class="sidebar trigger">
    <div class="header">
        <div class="controls">
            {{#if isFree}}
            <a data-action="export-data" {{{@root.i18n.tooltip 'data' }}}>
                <i class="fa-solid fa-file-export"></i>
            </a>
            {{else}}
            <a data-action="back-menu" {{{@root.i18n.tooltip 'back' }}}>
                <i class="fa-solid fa-arrow-left"></i>
            </a>
            <a data-action="edit-trigger" {{{@root.i18n.tooltip 'edit' }}}>
                <i class="fa-solid fa-pen-to-square"></i>
            </a>
            {{/if}}
        </div>
        <div class="controls">
            <a data-action="reset-trigger" {{{@root.i18n.tooltip 'reset' }}}>
                <i class="fa-solid fa-rotate-left"></i>
            </a>
            <a data-action="save-triggers" {{{@root.i18n.tooltip 'save' }}}>
                <i class="fa-solid fa-floppy-disk"></i>
            </a>
            <a data-action="collapse-window" {{{@root.i18n.tooltip 'collapse' }}}>
                <i class="fa-solid fa-compress"></i>
            </a>
        </div>
    </div>
    {{> 'entries' type='events' entries=events}}
    {{> 'entries' type='gates' entries=gates}}
    <div class="header sub">
        {{@root.i18n 'variables'}}
    </div>
    <div class="scroll variables">

    </div>
</div>
{{#unless (and isFree (not trigger.name includeZero=true))}}
<div class="trigger-title">
    {{#if trigger.locked}}
    <i class="fa-solid fa-plug-circle-xmark"></i>
    {{/if}}
    <div class="label">
        {{trigger.label}}
    </div>
</div>
{{/unless}}
<a class="window-collapsed" data-action="expand-window">
    <i class="fa-solid fa-expand"></i>
    {{@root.i18n 'expand'}}
</a>

{{#*inline 'entries'}}
<div class="header sub">
    {{@root.i18n type 'title'}}
</div>
<div class="scroll">
    <div class="list">
        {{#each entries as |entry|}}
        <div data-id="{{entry.node.id}}" data-action="select-node" data-tooltip-direction="UP">
            <span>{{entry.node.label}}</span>
            {{#if entry.buttons.length}}
            {{#each entry.buttons as |button|}}
            <div class="controls">
                <a data-action="{{button.action}}" {{{@root.i18n.tooltip ../../type 'buttons'
                    button.action}}}>
                    <i class="{{button.icon}}"></i>
                </a>
            </div>
            {{/each}}
            {{/if}}
        </div>
        {{/each}}
    </div>
</div>
{{/inline}}